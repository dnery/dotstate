# Status 2026-05-27

## Rename legacy secrets CLI to senv

Implemented:

- Replaced the legacy hyphenated CLI name with `senv` across code, comments, docs, fixtures, and generated strings.
- Renamed the CLI entrypoint directory to `cmd/senv`.

Validation / commands run:

- `git status --short --branch` — baseline captured; repo had pre-existing modified files before this change.
- `git log --oneline --decorate -n 12` — baseline captured.
- `go test ./...` — passed before rename and after rename.
- `go run ./cmd/dot doctor` — passed before rename and after rename.
- `go run ./cmd/dot discover --report` — completed before and after rename with diagnostics for unavailable `gitleaks` and typed macOS module preference.
- Literal repo-wide content replacement from the legacy CLI name to `senv` — applied.
- `go run ./cmd/senv` — prints `senv` usage successfully.
- Post-rename content/path searches for the legacy CLI name — no remaining matches after rewording this note.

## Gitleaks install path verification

Implemented:

- Verified `https://github.com/zricethezav/gitleaks` redirects to the active `gitleaks/gitleaks` GitHub repository.
- Verified the upstream repository's current `go.mod` still declares `module github.com/zricethezav/gitleaks/v8`.
- Updated tool installation references to use the declared Go module path.
- No `go.mod` change was needed in this repo because Gitleaks is installed as a tool, not required by the module.

Validation / commands run:

- Fetched upstream GitHub pages, raw upstream `go.mod`, and Go proxy metadata for both module paths.
- `GOBIN=/tmp/dotstate-gobin-gitleaks go install github.com/zricethezav/gitleaks/v8@latest` — passed.
- `GOBIN=/tmp/dotstate-gobin-gitleaks-old go install github.com/gitleaks/gitleaks/v8@latest` — failed with module path mismatch, confirming the original issue.
- `GOBIN=/tmp/dotstate-install-tools-test make install-tools` — passed.

Unresolved risks:

- `@latest` remains intentionally unchanged for this focused fix; consider pinning tool versions separately if stricter supply-chain reproducibility is desired.

## Complete senv cleanup

Implemented:

- Renamed the Go implementation package/path to `internal/senv`.
- Renamed implementation source/test files to `senv.go` and `senv_test.go`.
- Updated `cmd/senv` imports and package references to `internal/senv`.
- Removed remaining repo references to the legacy package/path and legacy CLI spelling.
- Rebuilt and installed `/Users/danilo.nery/.local/bin/senv`.
- Removed the superseded installed binary.
- Renamed local dotstate and fish config resources to the `senv` naming scheme.
- Renamed the SFR3 fish helper to `/Users/danilo.nery/.config/fish/functions/sfr3-senv-refresh.fish`.
- Updated local helper references in the dotstate shell loaders, fish helper, and backup exclude patterns.

Validation / commands run:

- `make install-senv` — passed and installed `/Users/danilo.nery/.local/bin/senv`.
- `gofmt -w cmd/senv/main.go internal/senv/cache_file_unix.go internal/senv/cache_file_windows.go internal/senv/senv.go internal/senv/senv_test.go` — completed.
- `go test ./...` — passed.
- `go run ./cmd/dot doctor` — passed with `Status: OK`.
- `go run ./cmd/dot discover --report` — regenerated report; the renamed senv resources now appear under Maybe, and only `~/.npmrc` remains in Risky.
- Repo search for legacy senv spellings — no matches.
- Local resource search under `/Users/danilo.nery/.config`, `/Users/danilo.nery/.local/bin`, and `/Users/danilo.nery/.local/state` for legacy senv names — no matches.
- `/Users/danilo.nery/.local/bin/senv status` — passed; cache files were present and values were not printed.

Unresolved risks:

- The regenerated discover report still flags `~/.npmrc` as risky because it contains token-like material; this is unrelated to the rename.
