# Status 2026-05-13

## Module/state contract goal

Pursued the first macOS-first goal from `docs/2026-05-13-goals.md`: specify the module/state contract and macOS state surface contract, then update the remaining goals based on that contract.

## Commands run

- `git status --short --branch` / `git log --oneline --decorate -n 12` — baseline showed `master` ahead 1, behind 2 with an existing dirty working tree.
- `go test ./...` — passed during baseline.
- `go run ./cmd/dot doctor` — passed, `Status: OK` on `darwin/arm64`.
- `go run ./cmd/dot discover --report` — completed; output was saved to a temporary file because the report is very large and may include sensitive local paths/findings.
- `make docs-check` — passed after adding the new spec links.
- `git diff --check` — passed.
- `go test ./...` — passed after documentation updates.
- `make docs-check` — passed after adding the dated status note.
- `git diff --check` — passed after adding the dated status note.
- Python completion audit script — passed checks for required spec files, lifecycle phases, versioned schemas, common fields, capabilities, redaction fixture requirements, macOS surfaces, goal removal, hindsight updates, and docs index links.

## Implemented

- Added `docs/specs/modules.md` with shared lifecycle, versioned fact/plan/result/diagnostic/backup/audit schemas, common fields, capabilities, redaction rules, UX wording, desired-state artifact conventions, fixture layout, and implementation gates.
- Added `docs/specs/macos-state-surfaces.md` with macOS-first surface contracts for files, brew, mas, apps, defaults, launchd, profiles, privacy/TCC, subrepos, and secrets.
- Linked the new specs from `README.md` and `docs/README.md`.
- Updated `docs/2026-05-13-goals.md` to remove the completed spec goal, record the contract baseline, and revise upcoming goals to depend on the shared schemas/fixtures/capability gates.

## Module orchestrator goal

Pursued the second listed goal from `docs/2026-05-13-goals.md`: turn apply/capture/sync into a module orchestrator, then update the remaining goals based on that implementation.

## Additional commands run

- `git add -A && git commit -m "Checkpoint macOS module contract and hardening"` — committed the existing dirty tree as requested before starting new work.
- `go test ./internal/modules ./internal/sync ./internal/cli` — passed after introducing the orchestrator.
- `go test ./...` — passed after the orchestrator implementation.
- `make docs-check` — passed after CLI/spec docs updates.
- `git diff --check` — passed after CLI/spec docs updates.
- `go run ./cmd/dot apply --dry-run` — emitted an apply plan without mutation.
- `go run ./cmd/dot capture --dry-run` — emitted a capture plan without mutation.
- `git add -A && git commit -m "Introduce module orchestrator for apply and sync"` — committed the first orchestrator slice.
- `go test ./internal/modules ./internal/sync` — passed after strengthening apply verification.
- `go test ./...` — passed after strengthening apply verification.
- `make docs-check` — passed after strengthening apply verification.
- `git diff --check` — passed after strengthening apply verification.
- `git add -A && git commit -m "Enforce files module apply verification"` — committed verification/restore test hardening.
- `go run ./cmd/dot apply --dry-run` — emitted an apply plan without mutation from a clean working tree.
- `go run ./cmd/dot capture --dry-run` — emitted a capture plan without mutation from a clean working tree.
- `go run ./cmd/dot sync --dry-run` — emitted capture/apply plans without mutation from a clean working tree.
- `make test-e2e-fast` — passed discover-fast with 0 failing checks; latest artifact under `state/e2e-runs/20260513T020353Z/`.
- Final `go test ./...` — passed during completion audit.
- Final `make docs-check` — passed during completion audit.
- Final `go run ./cmd/dot apply --dry-run`, `go run ./cmd/dot capture --dry-run`, and `go run ./cmd/dot sync --dry-run` — emitted plans without mutating the repo.
- Completion audit script — passed checks mapping the Goal 2 requirements to concrete code, docs, tests, dry-run output, and the updated goals document.

## Additional implemented

- Added `internal/modules` with shared schema records, capabilities, lifecycle phase constants, plan summaries, an orchestrator, and Chezmoi-backed `files` module.
- Routed `dot apply`, `dot capture`, and `dot sync` through the module orchestrator.
- Added `--dry-run` to `dot apply`, `dot capture`, and `dot sync` with concise plan output.
- Added files-module backup before apply, restore support, post-apply diff verification, and no-op planning when diff is empty.
- Added orchestrator blocking for mutating changes that do not expose `auto_apply`.
- Hardened `dot sync` to refuse a dirty repo before capture and report pull/rebase conflicts with porcelain status and recovery guidance.
- Added unit tests for dry-run planning, backup/restore, apply verification, dry-run-only blocking, sync dirty-state refusal, sync dry-run non-mutation, and pull conflict reporting.
- Updated CLI reference, sync spec, architecture docs, and the goals document to reflect the orchestrator baseline.

## macOS bootstrap and scheduling goal

Pursued the third listed goal from `docs/2026-05-13-goals.md`: make macOS bootstrap and scheduling first-class flows, then update the remaining goals based on that implementation.

## Bootstrap/scheduling commands run

- `git status --short --branch` / `git log --oneline --decorate -n 12` — session baseline showed `master` ahead 6, behind 2 and a clean working tree.
- `go test ./...` — passed during baseline.
- `go run ./cmd/dot doctor` — passed, `Status: OK` on `darwin/arm64`.
- `go run ./cmd/dot discover --report` — completed; output was saved to a temporary file because the report is very large and may include sensitive local paths/findings.
- `go test ./internal/schedule ./internal/macos ./internal/cli` — passed after adding schedule/audit command surfaces.
- `go test ./...` — passed after adding schedule/audit command surfaces.
- `sh -n scripts/bootstrap-macos.sh` — passed shell syntax validation.
- `scripts/bootstrap-macos.sh --dry-run --repo https://github.com/dnery/dotstate` — printed the release asset URL, install path, Xcode/Homebrew checks, 1Password/op checkpoint, bootstrap command, and validation commands without mutation.
- `GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build ... ./cmd/dot` — produced a Mach-O arm64 binary in a temporary directory.
- `go run ./cmd/dot macos audit --json | python3 -m json.tool` — emitted valid `dotstate.audit.v1` JSON with a redacted host and bootstrap capability diagnostics.
- `go run ./cmd/dot schedule --help` — listed `install`, `status`, and `remove` subcommands.
- `go run ./cmd/dot schedule install --dry-run --dot-bin /tmp/dot` — printed the LaunchAgent plan without writing or loading it.
- `go run ./cmd/dot schedule status` — reported the LaunchAgent as not installed without mutation.
- `make docs-check` — passed after bootstrap/scheduling docs updates.
- `git diff --check` — passed after bootstrap/scheduling docs updates.
- `make test-e2e-fast` — passed discover-fast with 0 failing checks; latest artifact under `state/e2e-runs/20260513T023119Z/`.
- Final completion audit script — passed checks mapping the Goal 3 requirements to release workflow assets, bootstrap script/CLI checkpoints, schedule commands, audit JSON, docs, tests, and updated goals.

## Bootstrap/scheduling implemented

- Added `.github/workflows/release.yml` to publish `dot-darwin-arm64.tar.gz` and `SHA256SUMS` on release tags or manual workflow dispatch.
- Added `scripts/bootstrap-macos.sh` for one-line Apple Silicon bootstrap with dry-run support, release download, Xcode/Homebrew checks, and a manual 1Password/op checkpoint.
- Enhanced `dot bootstrap` to use repo config when available, print macOS prerequisite checkpoints, keep 1Password/op unlock manual, and point users to safe validation commands.
- Added a bootstrap-safe `dot macos audit --json` bridge that emits `dotstate.audit.v1`, redacts hostnames, and reports pending macOS surfaces as capability diagnostics.
- Added `internal/schedule` and `dot schedule install|status|remove` for a macOS user LaunchAgent at `~/Library/LaunchAgents/com.dnery.dotstate.sync.plist`.
- Added schedule dry-run, no-load, interval override, launchd PATH/log configuration, best-effort load/unload, and non-dotstate plist overwrite/remove protections.
- Documented the release asset, one-line bootstrap, manual checkpoints, schedule command, LaunchAgent contract, and macOS shutdown limitation.
- Updated `docs/2026-05-13-goals.md` to remove the completed bootstrap/scheduling goal, record the new baseline, and revise upcoming goals with hindsight.

## Redaction, secrets, and macOS privacy diagnostics goal

Pursued the fourth listed goal from `docs/2026-05-13-goals.md`: harden redaction, secret handling, and macOS privacy diagnostics, then update the remaining goals based on that implementation.

## Redaction/privacy commands run

- `git status --short --branch` / `git log --oneline --decorate -n 12` — session baseline showed `master` ahead 12, behind 2 and a clean working tree.
- `go test ./...` — passed during baseline.
- `go run ./cmd/dot doctor` — passed, `Status: OK` on `darwin/arm64`.
- `go run ./cmd/dot discover --report` — completed; output was saved to a temporary file because the report is very large and may include sensitive local paths/findings.
- `go test ./internal/redact`, `go test ./internal/modules`, `go test ./internal/runner ./internal/logging`, and `go test ./internal/discover` — passed after the respective implementation slices.
- `sh -n scripts/bootstrap-macos.sh` — passed after adding shell-output redaction.
- `go test ./internal/cli ./internal/macos ./internal/modules` — passed after CLI/audit/privacy diagnostics updates.
- `go test ./...` — passed after code and docs updates.
- `make docs-check` — passed after docs/goals updates.
- `git diff --check` — passed after docs/goals updates.
- Final redaction smoke checks — `scripts/bootstrap-macos.sh --dry-run`, `dot bootstrap --skip-op-checkpoint`, `dot schedule install --dry-run`, and `dot macos audit --json` all rejected the `DOTSTATE_TEST_SECRET_DO_NOT_PRINT` sentinel in output.
- Final `go run ./cmd/dot apply --dry-run` and `go run ./cmd/dot sync --dry-run` — emitted sanitized plans without mutation.
- `make test-e2e-fast` — passed discover-fast with 0 failing checks; latest artifact under `state/e2e-runs/20260513T030104Z/`.
- Final completion audit script — passed checks mapping the Goal 4 requirements to gitleaks parsing/unavailable diagnostics, redaction/sensitivity propagation, Keychain reference-only behavior, permission diagnostics, sentinel tests, command output, docs, and updated goals.

## Redaction/privacy implemented

- Added `internal/redact` as the shared sanitizer for sentinel values, known secret patterns, credentialed URLs, `op://` references, and restricted macOS hints.
- Sanitized module plans/run reports before return and propagated sensitivity through changes, diagnostics, backups, and results.
- Marked files-module backup records as `secret` when local restorable payloads contain secret-looking content, without serializing payload contents in records.
- Redacted runner errors and structured logs before display/storage.
- Replaced the gitleaks placeholder path with parsed gitleaks JSON output and added `secrets.gitleaks.unavailable` diagnostics for redacted discover reports.
- Redacted discover reports, bootstrap CLI output, bootstrap script dry-run output, schedule plans, and run-report rendering.
- Added macOS privacy guardrail diagnostics for Full Disk Access, TCC, SIP, MDM, and Keychain reference-only behavior.
- Added permission diagnostics that classify Full Disk Access, TCC prompt, sudo/privacy, SIP, and MDM failures with remediation and capability labels.
- Added sentinel tests proving discover reports, module plans/results/backups, runner errors, logs, bootstrap output, schedule plans, and audit JSON do not print `DOTSTATE_TEST_SECRET_DO_NOT_PRINT`.
- Updated CLI reference, discover/modules/macOS-surface specs, architecture docs, and the goals document to reflect the new redaction/privacy baseline.

## macOS verification goal

Pursued the fifth requested goal from `docs/2026-05-13-goals.md`. The file had been renumbered after the redaction/privacy pass, so the active implementation target was the remaining verification goal: expand repeatable fixtures and macOS behavior checks.

## Verification commands run

- `git status --short --branch` / `git log --oneline --decorate -n 12` — session baseline showed `master` ahead 19, behind 2 and a clean working tree.
- `go test ./...` — passed during baseline.
- `go run ./cmd/dot doctor` — passed, `Status: OK` on `darwin/arm64`.
- `go run ./cmd/dot discover --report` — completed; output was saved to a temporary file because the report is very large and may include sensitive local paths/findings.
- `DOTSTATE_UPDATE_GOLDEN=1 go test ./internal/modules ./internal/macos` — generated new fixture goldens.
- `go test ./internal/modules ./internal/macos` — passed after fixture/golden tests.
- `go test ./...` — passed after verification slices.
- `make docs-check` — passed after verification docs updates.
- `git diff --check` — passed after verification docs updates.
- `bash -n test/e2e/discover_harness.sh` and `bash -n test/e2e/verify_artifacts_no_sentinel.sh` — passed shell syntax checks.
- `make test-e2e-verify` — passed `macos-verification` with 0 failing checks; first artifact under `state/e2e-runs/20260513T032133Z/`.
- Final validation: `go test ./...`, `make docs-check`, `git diff --check`, `bash -n test/e2e/discover_harness.sh`, and `bash -n test/e2e/verify_artifacts_no_sentinel.sh` all passed.
- Final `make test-e2e-fast` — passed discover-fast with 0 failing checks; latest artifact under `state/e2e-runs/20260513T032550Z/`.
- Final `make test-e2e-verify` — passed `macos-verification` with 0 failing checks; latest artifact under `state/e2e-runs/20260513T032552Z/`.
- Final completion audit script — passed checks mapping the requested verification goal to fixtures, golden tests, e2e artifacts, runner probes, idempotency, interrupted restore, docs/goals cleanup, and validation commands.

## Verification implemented

- Added the v1 module fixture layout under `test/fixtures/modules/v1/` with files and bootstrap-audit cases.
- Added golden tests for files facts, apply plans, redaction, path normalization, permission diagnostics, audit envelopes, and orchestrator result records.
- Added read-only macOS runner probes and tests for Brewfile checks, curated `defaults read`, and user LaunchAgent `launchctl print` command shapes.
- Added `test/e2e/verify_artifacts_no_sentinel.sh` and wired it into the harness so generated review artifacts fail on sentinel leaks.
- Added `macos-verification` harness coverage for sandbox-home apply dry-run, apply, backup payload creation, idempotent second apply, capture dry-run, capture, schedule install/status/remove in a sandbox, and release bootstrap dry-run.
- Added an interrupted-apply restore test proving secret-tainted backup records remain share-safe while the local payload remains restorable.
- Documented the verification fixture baseline in the module/macOS specs and e2e harness README.

## Read-only macOS audit goal

Started pursuing the remaining goals as a final-state roadmap rather than by mutable numbering. Implemented the read-only audit slice first because later discovery and capture work depends on typed facts.

## Read-only audit commands run

- Baseline: `git status --short --branch`, `git log --oneline --decorate -n 12`, `go test ./...`, `go run ./cmd/dot doctor`, `go run ./cmd/dot discover --report` redirected to a local file to avoid printing sensitive machine findings.
- `make docs-check` and `git diff --check` — passed at baseline.
- Context7 checked Homebrew documentation for read-only `brew tap`, `brew list --formula/--cask --versions`, and `brew bundle check` command shapes.
- `DOTSTATE_UPDATE_GOLDEN=1 go test ./internal/macos` — refreshed macOS audit goldens.
- `go test ./internal/macos ./internal/modules ./internal/cli` — passed after adding audit modules.
- `go test ./...` — passed after wiring `dot macos audit --json` to the full read-only collectors.
- Smoke: `go run ./cmd/dot macos audit --json` redirected to a temp file, parsed as JSON, and reported `dotstate.audit.v1` with 352 facts, 8 diagnostics, and no hard errors on this machine.

## Read-only audit implemented

- `dot macos audit --json` now uses the module audit interface and emits real facts instead of pending surface diagnostics.
- Facts cover files, Homebrew taps/formulae/casks/Brewfile presence, `mas` inventory when available, installed app bundles, user LaunchAgents, Homebrew services, curated defaults, profiles/MDM posture, privacy/TCC manual checkpoints, subrepo manifest presence, and Keychain/secret reference-only policy.
- Missing tools, unavailable accounts, unreadable plists, absent defaults, privacy constraints, and MDM/report-only posture are diagnostics rather than command failures.
- Added redaction for audit facts and diagnostics through shared module sanitizers.
- Added golden fixture `test/fixtures/modules/v1/macos/audit-readonly/` and tests for unavailable tools and sentinel redaction.
- Updated CLI/bootstrap/spec docs to describe the implemented read-only audit behavior.

## Discovery/onboarding goal

Reduced default discovery noise after the read-only audit slice, keeping broad filesystem scans as explicit opt-in.

## Discovery commands run

- `go test ./internal/discover ./internal/cli` — passed after adding curated roots, registries, filters, and CLI flags.
- `go run ./cmd/dot discover --report` redirected to a temp file — default real-machine report dropped from 30,556 lines / 13,882 Maybe / 498 Risky to 111 lines / 31 Maybe / 3 Risky, with typed-module guidance and no mutation.
- `go run ./cmd/dot discover --report --roots <temp> --max-file-size 16` — confirmed explicit roots work and large non-config files are filtered.
- `go test ./...`, `make docs-check`, `git diff --check`, and `make test-e2e-fast` — passed after discovery changes; latest fast artifact under `state/e2e-runs/20260513T044244Z/`.

## Discovery implemented

- Default roots now use curated dotfiles, curated XDG config files, curated editor config files, and user `state/discover/curated-roots.txt`; broad `~/.config` and `~/Library` roots require `--deep` or `--roots`.
- Added `dot discover --roots` and `--max-file-size` flags.
- Added `state/discover/ignore.txt` support for user-maintained glob/substring excludes.
- Excluded caches, language-server payloads, package-manager/vendor trees, build outputs, source maps, browser databases, and generated `.app` bundles by default.
- Added typed-module guidance pointing non-file macOS state toward `dot macos audit --json` and future module capture flows instead of crawler expansion.
- Enhanced the interactive review text UI to explain Recommended/Maybe/Risky/Ignored categories and why typed macOS module facts are preferred.
- Preserved the dirty-repo guard before discover auto-commit.

## Capture/reconcile goal

Implemented capture-first non-file modules after discovery noise reduction. The implementation intentionally mutates the repo for portable artifacts but keeps system-level non-file apply manual/dry-run-only except for clone-if-missing subrepos.

## Capture/reconcile commands run

- `go test ./internal/macos ./internal/cli` — passed after adding non-file artifact modules and `dot subrepo status`.
- `DOTSTATE_UPDATE_GOLDEN=1 go test ./internal/macos` — generated capture artifact goldens under `test/fixtures/modules/v1/macos/capture-artifacts/`.
- `go run ./cmd/dot capture --dry-run` — rendered a module plan with non-file artifact creates/updates and no repo mutation.
- `go run ./cmd/dot subrepo status` — reported no declared subrepos for the current machine without mutation.
- `go test ./...`, `make docs-check`, `git diff --check`, `bash -n test/e2e/discover_harness.sh`, and `make test-e2e-verify` — passed after capture/reconcile changes; first verification artifact under `state/e2e-runs/20260513T045645Z/`.
- Final validation after commits: `go test ./...`, `make docs-check`, `git diff --check`, `bash -n test/e2e/discover_harness.sh`, `bash -n test/e2e/verify_artifacts_no_sentinel.sh`, `go run ./cmd/dot doctor`, `go run ./cmd/dot macos audit --json`, `go run ./cmd/dot discover --report`, `go run ./cmd/dot capture --dry-run`, `go run ./cmd/dot subrepo status`, `make test-e2e-fast`, and `make test-e2e-verify` all passed.
- Final `make test-e2e-fast` — passed with 0 failing checks; latest artifact under `state/e2e-runs/20260513T050005Z/`.
- Final `make test-e2e-verify` — passed with 0 failing checks; latest artifact under `state/e2e-runs/20260513T050007Z/`.

## Capture/reconcile implemented

- `dot capture` now includes macOS non-file modules in the shared orchestrator.
- Capture writes reviewable artifacts: `state/macos/brew/Brewfile`, `state/macos/mas.toml`, `state/macos/apps.toml`, `state/macos/defaults.toml`, and `state/secrets/generated.toml`.
- Capture plans/results store artifact paths and hashes, not full generated content, so reports remain share-safe.
- Apply plans for Brewfile/MAS/apps/defaults/secrets artifacts are manual/dry-run-only until native rollback/idempotency semantics are implemented.
- `state/subrepos.toml` now has clone-if-missing apply support and `dot subrepo status`; existing non-git destinations remain manual.
- The e2e macOS verification harness now checks that capture writes a secret generated-file policy artifact with `secret_values_serialized = false`.

## README onboarding update

Rewrote the top-level `README.md` as a self-contained onboarding guide for unfamiliar but tech-savvy users.

## README commands run

- Baseline: `git status --short --branch`, `git log --oneline --decorate -n 12`, `go test ./...`, `go run ./cmd/dot doctor`, and `go run ./cmd/dot discover --report` redirected to `/tmp/dotstate-readme-baseline/discover-report.txt` to avoid printing sensitive local findings.
- `make docs-check`, `git diff --check`, and `go test ./...` — passed after the README rewrite.

## README content covered

- Product purpose, macOS-first status, implemented vs conservative/manual surfaces, and safety model.
- Mental model for audit/discover/capture/apply/sync and a mutation table explaining what touches the machine vs repo.
- Fresh macOS bootstrap flow with dry-run, manual checkpoints, and validation commands.
- Existing-machine onboarding flow using audit, curated discover, capture, sync, and subrepo commands.
- Secrets/privacy guarantees and warnings about sharing redacted local reports.
- Repository layout, important state paths, development commands, e2e harnesses, golden fixture updates, and documentation map.

## Unresolved risks

- `master` remains ahead of and behind `origin/master`; validation reflects the local branch state.
- The GitHub Release workflow is validated by local cross-build and static inspection, but actual asset publication still requires a tag/manual Actions run with repository permissions.
- The files module currently backs up all managed files before a non-empty Chezmoi diff because Chezmoi diff output is intentionally not parsed or printed for safety.
- Secret-tainted backup payloads remain local and restorable; future restore UX still needs artifact-level verification so users do not accidentally share payload directories.
- macOS Tahoe-specific edge cases still need continued verification as non-file capture/apply modules are implemented.
