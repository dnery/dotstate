# Status 2026-05-12

## Review-fix session

Implemented fixes from the latest review summary across discovery, `secrets-env`, CI harnessing, and cross-platform cache handling.

## Commands run

- `git status --short --branch` / `git log --oneline --decorate -n 12` — baseline showed `master` ahead 1, behind 2.
- `go test ./...` — passed before changes and after changes.
- `go run ./cmd/dot doctor` — passed, `Status: OK`.
- `go run ./cmd/dot discover --report` — passed after translating per-candidate secret scan failures into risky warnings; report output is large and may include sensitive local paths/findings.
- `GOTOOLCHAIN=go1.23.12 go test ./internal/discover` — passed, confirming declared Go 1.23 compatibility.
- `GOOS=windows GOARCH=amd64 go test -c ...` for `internal/secretsenv`, `internal/discover`, and `internal/chez` — cross-compiled test binaries successfully.
- `go vet ./...` — passed.
- `make test-e2e-fast` — passed with 0 failing checks; latest artifact under `state/e2e-runs/20260512T233336Z/`.
- `make docs-check` — passed after renaming dated notes from `stocktake-*` to `status-*`.
- `go test ./...` — passed after addressing follow-up review findings.
- `GOTOOLCHAIN=go1.23.12 go test ./internal/discover ./internal/secretsenv` — passed.
- `go vet ./...` — passed.
- `GOOS=windows GOARCH=amd64 go test -c ...` for `internal/secretsenv` and `internal/discover` — cross-compiled test binaries successfully.
- `go run ./cmd/dot doctor` — passed, `Status: OK`.
- `go run ./cmd/dot discover --report` — passed; report remains large and includes explicit risky warnings for unreadable local files.
- `make test-e2e-fast` — passed with 0 failing checks; latest artifact under `state/e2e-runs/20260513T010733Z/`.
- `make docs-check` and `git diff --check` — passed.

## Implemented

- Removed Go 1.24+ test API use while keeping `go 1.23` declared support.
- Tightened section-scoped secret exports.
- Made Secure Note migration lookup prefer stable IDs and reject wrong-category/ambiguous title targets.
- Redacted credentialed subrepo remotes and merged existing subrepo manifest entries.
- Enabled strict mode for CI harness smoke workflows.
- Sandboxed Windows `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`, and related home variables in the Windows harness.
- Validated `secrets-env` scope names and reserved the aggregate cache name.
- Enforced private cache directory modes and added Windows-safe cache replacement.
- Skipped non-regular files before discovery processing and secret scanning.
- Increased secret scanner buffer and made scan failures explicit risky candidate warnings at the discovery reporting boundary.
- Normalized chezmoi managed destination paths and invoked `chezmoi managed` with the configured source.
- Preserved child command exit statuses for `secrets-env run`.
- Fixed bare numeric interactive selections to toggle.
- Renamed dated working notes from `docs/stocktake-*.md` to `docs/status-*.md` and updated `AGENTS.md`.
- Restored concealed 1Password migration field typing for common secret abbreviations (`PASS`, `PWD`, `KEY`, etc.) while keeping public ID/URL labels as text.
- Preserved configured local-note sections during `refresh --all`, including multiple configured scopes pointing at the same note.
- Made auto-discovered local-note cache names deterministic and collision-safe against configured/reserved names.
- Made malformed credentialed HTTPS remotes redact credentials instead of failing open.
- Made `secrets-env status` return cache directory/read errors loudly instead of reporting incomplete success.

## Unresolved risks

- `go.mod` remains at `go 1.23` intentionally. Bumping the declared toolchain would drop current compatibility and should be a separate policy decision.
- `dot discover --report` can still produce very large local reports; secret values are redacted, but paths and filenames may be sensitive.
